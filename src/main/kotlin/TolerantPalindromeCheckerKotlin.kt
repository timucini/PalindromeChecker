import java.util.*

/**
 * Given a string, return true if the string is a palindrome or false if it is not.
 * Palindromes are strings that form the same word if it is reversed.
 * Include spaces and punctuation in determining if the string is a palindrome.
 *
 *
 * In this exercise we will allow bad characters. Our string will be (tolerant) palindrome if after removing characters,
 * like special characters (f.e. "!", "#" , "&", "=") and blank spaces (" "), string is a palindrome.
 *
 * String is not! case case-sensitive.
 */

class TolerantPalindromeCheckerKotlin {

    fun isTolerantPalindrome(str: String): Boolean {
        TODO("not implemented")
    }
}

